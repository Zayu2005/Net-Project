# 网络数据传输与封装模拟系统

## 项目简介

网络数据传输与封装模拟系统是一个基于 Web 的网络教学与演示平台，用于可视化展示计算机网络中的数据传输过程、路由算法、OSI七层协议封装等核心概念。该系统采用前后端分离架构，提供直观的图形化界面和实时动画效果，帮助用户理解网络数据传输的底层机制。

### 核心功能

- **网络拓扑管理**：支持创建、编辑和管理网络节点（路由器、交换机、主机）及链路
- **三层网络架构**：清晰展示核心层、汇聚层、接入层的层次化网络结构
- **路由算法模拟**：支持 Dijkstra、OSPF、RIP、BGP 等多种路由算法
- **数据传输模拟**：模拟 TCP、UDP、HTTP 等协议的数据传输过程
- **OSI 七层封装可视化**：动态展示应用层到物理层的数据封装过程
- **路径动画演示**：实时动画展示数据包在网络节点间的跳转过程
- **传输记录查询**：完整记录每次传输的路径、延迟、跳数等详细信息

### 技术特色

- 🎨 **可视化设计**：基于 ECharts 和 D3.js 的高性能图形渲染
- 🚀 **实时动画**：流畅的数据包传输动画和路径高亮显示
- 📊 **数据驱动**：完整的数据库记录支持历史查询和统计分析
- 🔧 **易于扩展**：模块化设计，支持添加新的路由算法和协议
- 📱 **响应式布局**：适配不同屏幕尺寸的设备

## 技术架构

### 后端技术栈

| 技术 | 版本 | 说明 |
|-----|------|------|
| Spring Boot | 3.1.5 | 核心框架 |
| Java | 17 | 开发语言 |
| MyBatis Plus | 3.5.7 | ORM 框架 |
| MySQL | 8.0+ | 关系型数据库 |
| Redis | 7.0+ | 缓存数据库 |
| WebSocket | - | 实时通信 |
| Knife4j | 4.4.0 | API 文档 |
| Lombok | - | 代码简化工具 |
| Hutool | 5.8.24 | Java 工具库 |
| MapStruct | 1.5.5 | 对象映射工具 |

### 前端技术栈

| 技术 | 版本 | 说明 |
|-----|------|------|
| Vue | 3.4.0 | 渐进式框架 |
| Vite | 5.0.0 | 构建工具 |
| Vue Router | 4.2.5 | 路由管理 |
| Pinia | 2.1.7 | 状态管理 |
| TDesign | 1.9.0 | UI 组件库 |
| ECharts | 5.4.3 | 图表库 |
| D3.js | 7.8.5 | 数据可视化 |
| Axios | 1.6.2 | HTTP 客户端 |
| Sass | 1.69.5 | CSS 预处理器 |

### 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                      前端层 (Frontend)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ 拓扑管理页面 │  │ 传输模拟页面 │  │ 封装可视化  │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│         Vue 3 + Vite + TDesign + ECharts                │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/WebSocket
┌────────────────────▼────────────────────────────────────┐
│                    后端层 (Backend)                       │
│  ┌─────────────────────────────────────────────────┐   │
│  │              RESTful API 层                      │   │
│  │  NodeController │ LinkController │ RoutingController│
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │              业务逻辑层 (Service)                 │   │
│  │  路由算法 │ 封装模拟 │ 传输管理 │ 拓扑计算      │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │            数据访问层 (MyBatis Plus)              │   │
│  └─────────────────────────────────────────────────┘   │
│                Spring Boot 3.1.5                        │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────┐
│                   数据层 (Database)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │    MySQL     │  │    Redis     │  │   文件存储   │ │
│  │ 关系型数据库  │  │   缓存数据库  │  │   日志文件   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 数据库设计

### 核心数据表

1. **network_node** - 网络节点表
   - 存储路由器、交换机、主机等网络设备信息
   - 包含节点坐标、IP/MAC地址、状态等属性

2. **network_link** - 网络链路表
   - 存储节点间的连接关系
   - 记录带宽、延迟、丢包率等链路特性

3. **routing_table** - 路由表
   - 存储路由信息和下一跳
   - 支持多种路由协议

4. **packet_transmission** - 数据包传输记录表
   - 记录每次传输的完整信息
   - 包含路由路径、跳数、总延迟等统计数据

5. **packet_hop** - 数据包跳转记录表
   - 记录数据包在每个节点的跳转详情
   - 包含 TTL 值、封装数据等信息

6. **protocol_layer** - 协议封装层表
   - 存储 OSI 七层封装的详细信息
   - 记录每层的头部数据和负载大小

7. **system_config** - 系统配置表
   - 存储系统全局配置参数

### ER 图

```
┌─────────────────┐         ┌─────────────────┐
│  network_node   │         │  network_link   │
├─────────────────┤         ├─────────────────┤
│ + node_id (PK)  │◄───────►│ + link_id (PK)  │
│ - node_name     │         │ - source_node_id│
│ - ip_address    │         │ - target_node_id│
│ - mac_address   │         │ - bandwidth     │
│ - position_x    │         │ - delay         │
│ - position_y    │         │ - weight        │
│ - node_type     │         └─────────────────┘
└─────────────────┘
        │
        │
        ▼
┌─────────────────────────┐       ┌──────────────────┐
│  packet_transmission    │──────►│   packet_hop     │
├─────────────────────────┤       ├──────────────────┤
│ + transmission_id (PK)  │       │ + id (PK)        │
│ - source_node_id        │       │ - transmission_id│
│ - dest_node_id          │       │ - hop_sequence   │
│ - protocol              │       │ - current_node_id│
│ - routing_algorithm     │       │ - next_node_id   │
│ - routing_path          │       │ - delay          │
│ - hop_count             │       └──────────────────┘
│ - total_delay           │               │
└─────────────────────────┘               │
                                          ▼
                                  ┌──────────────────┐
                                  │ protocol_layer   │
                                  ├──────────────────┤
                                  │ + id (PK)        │
                                  │ - transmission_id│
                                  │ - layer_number   │
                                  │ - layer_name     │
                                  │ - header_data    │
                                  │ - payload_size   │
                                  └──────────────────┘
```

## 项目结构

```
Net-Code/
├── backend/                    # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/network/simulation/
│   │   │   │   ├── controller/      # 控制器层
│   │   │   │   ├── service/         # 业务逻辑层
│   │   │   │   ├── mapper/          # MyBatis Mapper
│   │   │   │   ├── entity/          # 实体类
│   │   │   │   ├── dto/             # 数据传输对象
│   │   │   │   ├── vo/              # 视图对象
│   │   │   │   ├── common/          # 公共类
│   │   │   │   ├── config/          # 配置类
│   │   │   │   └── algorithm/       # 路由算法实现
│   │   │   └── resources/
│   │   │       ├── db/              # 数据库脚本
│   │   │       │   ├── schema.sql   # 表结构
│   │   │       │   └── data.sql     # 初始数据
│   │   │       ├── mapper/          # MyBatis XML
│   │   │       └── application.yml  # 配置文件
│   │   └── test/                    # 测试代码
│   └── pom.xml                      # Maven 配置
│
├── frontend/                   # 前端项目
│   ├── src/
│   │   ├── api/                # API 接口
│   │   ├── assets/             # 静态资源
│   │   ├── components/         # 公共组件
│   │   │   ├── RouteTable.vue         # 路由表组件
│   │   │   └── EncapsulationVisualization.vue  # 封装可视化组件
│   │   ├── views/              # 页面组件
│   │   │   ├── TopologyManage.vue     # 拓扑管理页面
│   │   │   └── Simulation.vue         # 传输模拟页面
│   │   ├── router/             # 路由配置
│   │   ├── stores/             # Pinia 状态管理
│   │   ├── utils/              # 工具函数
│   │   ├── App.vue             # 根组件
│   │   └── main.js             # 入口文件
│   ├── public/                 # 公共资源
│   ├── package.json            # 依赖配置
│   └── vite.config.js          # Vite 配置
│
├── docs/                       # 文档目录
├── logs/                       # 日志目录
└── README.md                   # 项目说明文档
```

## 快速开始

### 环境要求

- **JDK**: 17+
- **Node.js**: 18+
- **MySQL**: 8.0+
- **Redis**: 7.0+
- **Maven**: 3.8+

### 后端启动

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd Net-Code/backend
   ```

2. **配置数据库**

   创建数据库并执行初始化脚本：
   ```bash
   mysql -u root -p < src/main/resources/db/schema.sql
   mysql -u root -p < src/main/resources/db/data.sql
   ```

3. **修改配置文件**

   编辑 `src/main/resources/application.yml`：
   ```yaml
   spring:
     datasource:
       url: jdbc:mysql://localhost:3306/network_simulation
       username: root
       password: your_password

     redis:
       host: localhost
       port: 6379
   ```

4. **启动后端服务**
   ```bash
   mvn clean install
   mvn spring-boot:run
   ```

5. **访问 API 文档**

   启动成功后访问：`http://localhost:8080/doc.html`

### 前端启动

1. **进入前端目录**
   ```bash
   cd Net-Code/frontend
   ```

2. **安装依赖**
   ```bash
   npm install
   # 或使用 pnpm
   pnpm install
   ```

3. **启动开发服务器**
   ```bash
   npm run dev
   ```

4. **访问应用**

   浏览器访问：`http://localhost:3000`

### Docker 部署（可选）

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f
```

## 功能演示

### 1. 网络拓扑管理

- 支持添加、编辑、删除网络节点（路由器、交换机、主机）
- 支持创建和管理节点间的链路连接
- 可视化展示三层网络架构：
  - **核心层 (y=100)**：路由器，提供高速骨干网连接
  - **汇聚层 (y=300)**：交换机，负责流量汇聚和分发
  - **接入层 (y=500)**：主机，终端用户设备

### 2. 路由算法模拟

支持多种经典路由算法：

- **Dijkstra**：最短路径优先算法
- **OSPF**：开放最短路径优先协议
- **RIP**：路由信息协议
- **BGP**：边界网关协议

实时计算并展示：
- 最优路径
- 路径长度和跳数
- 每跳延迟时间
- 路由决策过程

### 3. 数据传输模拟

- 选择源节点和目标节点
- 选择传输协议（TCP/UDP/HTTP）
- 输入要发送的数据内容
- 实时动画展示数据包传输过程
- 高亮显示传输路径和当前位置
- 显示每跳的延迟时间

### 4. OSI 七层封装可视化

动态展示数据在各层的封装过程：

| 层次 | 名称 | 功能 |
|-----|------|------|
| 7 | 应用层 | HTTP/FTP/DNS |
| 6 | 表示层 | 数据格式转换 |
| 5 | 会话层 | 会话管理 |
| 4 | 传输层 | TCP/UDP 端口 |
| 3 | 网络层 | IP 地址路由 |
| 2 | 数据链路层 | MAC 地址 |
| 1 | 物理层 | 物理传输 |

每层显示：
- 协议头部信息
- 数据封装过程
- 负载大小变化

### 5. 传输统计与分析

- 传输成功率统计
- 平均延迟时间
- 跳数分布
- 路径使用频率
- 协议使用分析

## 核心算法实现

### Dijkstra 最短路径算法

```java
public class DijkstraAlgorithm implements RoutingAlgorithm {
    @Override
    public RouteResult calculate(String sourceId, String destId) {
        // 初始化距离数组和访问标记
        Map<String, Integer> distances = new HashMap<>();
        Map<String, String> previous = new HashMap<>();
        Set<String> visited = new HashSet<>();
        PriorityQueue<Node> pq = new PriorityQueue<>();

        // 设置源节点距离为0
        distances.put(sourceId, 0);
        pq.offer(new Node(sourceId, 0));

        // 主循环
        while (!pq.isEmpty()) {
            Node current = pq.poll();
            if (visited.contains(current.id)) continue;
            visited.add(current.id);

            // 更新邻居节点距离
            for (Link link : getAdjacentLinks(current.id)) {
                String neighbor = link.getTargetId();
                int newDist = distances.get(current.id) + link.getWeight();

                if (newDist < distances.getOrDefault(neighbor, Integer.MAX_VALUE)) {
                    distances.put(neighbor, newDist);
                    previous.put(neighbor, current.id);
                    pq.offer(new Node(neighbor, newDist));
                }
            }
        }

        // 构建路径
        return buildPath(sourceId, destId, previous, distances);
    }
}
```

### 数据包封装流程

```java
public class PacketEncapsulation {
    public EncapsulationData encapsulate(String data, String protocol) {
        List<Layer> layers = new ArrayList<>();

        // 应用层
        layers.add(createApplicationLayer(data, protocol));

        // 传输层
        layers.add(createTransportLayer(protocol));

        // 网络层
        layers.add(createNetworkLayer());

        // 数据链路层
        layers.add(createDataLinkLayer());

        // 物理层
        layers.add(createPhysicalLayer());

        return new EncapsulationData(layers);
    }
}
```

## API 接口文档

### 节点管理 API

#### 获取所有节点
```http
GET /api/v1/nodes
Response: 200 OK
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "nodeId": "A",
      "nodeName": "核心路由器A",
      "ipAddress": "192.168.1.1",
      "macAddress": "AA:BB:CC:DD:EE:01",
      "positionX": 150,
      "positionY": 100,
      "nodeType": "ROUTER",
      "status": 1
    }
  ]
}
```

#### 创建节点
```http
POST /api/v1/nodes
Content-Type: application/json

{
  "nodeId": "X",
  "nodeName": "新节点X",
  "ipAddress": "192.168.1.100",
  "macAddress": "AA:BB:CC:DD:EE:64",
  "positionX": 400,
  "positionY": 300,
  "nodeType": "SWITCH"
}
```

### 路由计算 API

```http
POST /api/v1/routing/dijkstra/calculate
Content-Type: application/json

{
  "sourceNodeId": "A",
  "destNodeId": "N"
}

Response: 200 OK
{
  "code": 200,
  "data": {
    "path": ["A", "B", "F", "J"],
    "totalDistance": 7,
    "hopCount": 3,
    "pathDetails": [
      {
        "fromNode": "A",
        "toNode": "B",
        "weight": 3,
        "delay": 2,
        "bandwidth": 1000
      }
    ]
  }
}
```

### 数据传输 API

```http
POST /api/v1/transmission/start
Content-Type: application/json

{
  "sourceNodeId": "A",
  "destNodeId": "N",
  "routingAlgorithm": "DIJKSTRA",
  "protocol": "TCP",
  "dataContent": "Hello World"
}
```

完整 API 文档请访问：`http://localhost:8080/doc.html`

## 配置说明

### 后端配置 (application.yml)

```yaml
server:
  port: 8080

spring:
  application:
    name: network-simulation

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/network_simulation?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: your_password

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

knife4j:
  enable: true
  basic:
    enable: false
```

### 前端配置 (vite.config.js)

```javascript
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

## 性能优化

### 后端优化

1. **数据库优化**
   - 为频繁查询的字段添加索引
   - 使用 MyBatis Plus 的批量操作减少数据库访问
   - Redis 缓存热点数据

2. **算法优化**
   - 路由计算结果缓存
   - 使用优先队列优化 Dijkstra 算法
   - 异步处理传输记录

3. **并发控制**
   - 使用线程池处理并发请求
   - WebSocket 连接池管理

### 前端优化

1. **渲染优化**
   - ECharts 图表按需加载
   - 虚拟滚动优化大数据表格
   - 防抖节流优化动画性能

2. **资源优化**
   - 图片懒加载
   - 路由懒加载
   - Vite 构建优化

3. **缓存策略**
   - HTTP 缓存配置
   - Pinia 状态持久化

## 常见问题

### Q1: 数据库连接失败

**A:** 检查以下几点：
- MySQL 服务是否启动
- 数据库用户名密码是否正确
- 数据库 `network_simulation` 是否已创建
- 防火墙是否允许 3306 端口

### Q2: 前端无法连接后端

**A:** 确认：
- 后端服务是否正常启动（访问 http://localhost:8080）
- Vite 代理配置是否正确
- 浏览器控制台是否有 CORS 错误

### Q3: 拓扑图不显示

**A:** 可能原因：
- ECharts 未正确加载，检查网络
- 节点数据为空，检查数据库初始化
- 浏览器兼容性问题，建议使用 Chrome

### Q4: 动画卡顿

**A:** 优化建议：
- 减少节点和链路数量
- 降低动画帧率
- 使用性能更好的浏览器
- 关闭其他占用 GPU 的应用

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范

- **Java**: 遵循阿里巴巴 Java 开发手册
- **Vue**: 遵循 Vue.js 官方风格指南
- **Git Commit**: 使用语义化提交信息

## 开发路线图

### v1.1 (计划中)

- [ ] 支持 BGP 协议模拟
- [ ] 增加网络故障模拟功能
- [ ] 支持导入/导出拓扑配置
- [ ] 添加网络流量统计图表

### v1.2 (规划中)

- [ ] 支持多用户协作
- [ ] 增加实验报告生成功能
- [ ] 移动端适配
- [ ] 国际化支持

### v2.0 (长期规划)

- [ ] 支持 SDN 网络模拟
- [ ] 机器学习路由优化
- [ ] 3D 网络拓扑可视化
- [ ] 云端部署版本

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 联系方式

- 项目主页：[GitHub Repository]
- 问题反馈：[GitHub Issues]
- 技术支持：[support@example.com]

## 致谢

感谢以下开源项目：

- [Spring Boot](https://spring.io/projects/spring-boot)
- [Vue.js](https://vuejs.org/)
- [ECharts](https://echarts.apache.org/)
- [TDesign](https://tdesign.tencent.com/)
- [MyBatis Plus](https://baomidou.com/)

---

**注意**: 本项目仅用于教学和演示目的，不建议直接用于生产环境。
